attribute vec3 v_position;
varying vec3 color;
uniform mat4 mvpMatrix;

//trap hole color is black
void main(void) {
   color =  vec3(0.0f, 0.0f, 0.0f); //color is black
   gl_Position = mvpMatrix * vec4(v_position, 1.0);
 }
