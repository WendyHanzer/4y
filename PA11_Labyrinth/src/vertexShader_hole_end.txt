attribute vec3 v_position;
varying vec3 color;
uniform mat4 mvpMatrix;

//end hole color is red
void main(void) {
   color =  vec3(1.0f, 0.0f,0.0f); //color is red
   gl_Position = mvpMatrix * vec4(v_position, 1.0);
 }
