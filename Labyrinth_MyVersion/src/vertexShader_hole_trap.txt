attribute vec3 v_position;
varying vec3 color;
uniform mat4 mvpMatrix;

//trap hole color is black
void main() {
   color =  vec3(0.0, 0.0, 0.0); //color is black
   gl_Position = mvpMatrix * vec4(v_position.xyz, 1.0);
 }
